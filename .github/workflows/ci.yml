name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    # Explicit name so the check run is distinct from the `build` job in
    # deploy.yml - the main ruleset requires this one by name.
    name: Build and type-check
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v7
      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      # Restored after `npm ci`, which wipes node_modules before installing -
      # caching this beforehand would just get erased.
      - name: Cache Astro/Vite build cache
        uses: actions/cache@v6
        with:
          path: |
            .astro
            node_modules/.vite
          key: astro-build-cache-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            astro-build-cache-
      - name: Type-check
        run: npx astro check
      - name: Build
        run: npm run build
